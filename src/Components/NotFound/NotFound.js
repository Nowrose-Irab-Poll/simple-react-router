import React from 'react';

const NotFound = () => {
    return (
        <div>
            <h2>Page not found</h2>
            <p>Don't ask me where the page has gone.</p>
        </div>
    );
};

export default NotFound;